Create a complete local fine-tune setup folder. Include:
1. A customizable system_prompt.txt file (leave clear placeholders for name, personality, rules)
2. A requirements.txt
3. A train.py script for simple LoRA fine-tune on a small model
4. A start_finetune.bat that installs deps and runs training
Make everything ready to edit and run on Windows with one double-click.